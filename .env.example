# EmailJS – contact form sends email to your admin address
# 1. Sign up at https://www.emailjs.com/
# 2. Create an Email Service (e.g. Gmail) and note the SERVICE_ID
# 3. Create an Email Template with variables: {{name}}, {{email}}, {{message}} (optional: {{phone}}, {{address}})
# 4. Copy your Public Key from Account > API Keys
# 5. Create .env.local in this folder (or .env) and add:

REACT_APP_EMAILJS_SERVICE_ID=YOUR_SERVICE_ID
REACT_APP_EMAILJS_TEMPLATE_ID=YOUR_TEMPLATE_ID
REACT_APP_EMAILJS_PUBLIC_KEY=YOUR_PUBLIC_KEY
